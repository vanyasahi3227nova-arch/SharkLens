{
  "name": "policyforge-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API for PolicyForge AI clone",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "echo 'No build step required for backend'",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  }
}
